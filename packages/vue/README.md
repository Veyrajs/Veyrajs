# @annotacanvas/vue

Vue 3 adapter for AnnotaCanvas.

> **Status:** placeholder. Built in a later phase on top of `@annotacanvas/core`. Will
> provide declarative components, composables, batched prop→mutation updates, event
> re-emission, and an imperative escape hatch via template refs.
