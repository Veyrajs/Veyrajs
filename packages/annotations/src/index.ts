/**
 * `@annotacanvas/annotations` — annotation node types and controls.
 *
 * FUTURE ONLY. Not part of the MVP or V1 scope. This package is intentionally empty;
 * it exists to reserve the name and lock the plugin boundary.
 *
 * Annotation primitives (BBox, polygon, polyline, keypoint/skeleton, cuboid, brush mask,
 * rotated box, ...) will be implemented here purely against the core's public plugin /
 * control / hit-test / serializer extension points — with **zero changes** to
 * `@annotacanvas/core`. That zero-change property is the definition of success for the
 * whole foundation.
 */
export const VERSION = '0.0.0'
