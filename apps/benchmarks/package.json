{
  "name": "@annotacanvas/benchmarks",
  "version": "0.0.0",
  "private": true,
  "description": "AnnotaCanvas performance benchmarks: tinybench CPU micro-benchmarks + a manual canvas FPS harness.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "bench": "vitest bench --run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@annotacanvas/core": "workspace:*"
  },
  "devDependencies": {
    "happy-dom": "catalog:",
    "tinybench": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
